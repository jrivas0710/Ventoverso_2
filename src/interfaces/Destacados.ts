

export interface Destacado { //esto va a todos los clarinetes de pagina d_clarinetes
    id: string,
    imagenUrl : string,
    estrellas : number, 
    nombre: string,
    precio:number,
   
  } 